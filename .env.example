# Environment variables for ADO SuperUI
# Copy this to .env and adjust as needed

# Use local proxy (true) or direct API access (false)
VITE_USE_PROXY=true

# Proxy URL (only used when VITE_USE_PROXY=true)
VITE_PROXY_URL=http://localhost:3131
